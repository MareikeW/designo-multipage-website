import React from "react";

const DesignCard = ({title}) => {
    return (
        <div>
            <h3>{title}</h3>
            <button>View Project</button>
        </div>
    )
}

export default DesignCard;